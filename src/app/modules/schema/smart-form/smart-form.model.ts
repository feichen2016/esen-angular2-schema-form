
import { DataSourceType, InputType, ValueType } from '../../shared/components/base/option-enum';

export class SmartFormModel {
  constructor(
    id?: string,
    name?: string,
    displayName?: string,
    description?: string,
    jsonTemplate?: string,
    xmlTemplate?: string,
    code?: string,
    version?: number,
    // fields?: SmartFormFieldModel[],
  ) { }
}

